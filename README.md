# pull-zoom-course-recordings
Python code to pull information on Zoom recordings and summarize information to copy into a course announcement

## Setup

```
pip install playwright transformers torch
playwright install chromium
playwright install-deps chromium
```
